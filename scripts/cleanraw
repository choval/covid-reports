#!/usr/bin/env php
<?php
chdir(__DIR__ .'/../');

$files = glob('reports/*/*/raw.txt');
foreach ($files as $file) {
    $raw = file_get_contents($file);
    if (strpos($raw, 'PADDING CONTENT GENERATED BY COVID REPORTS')) {
        echo "REMOVING $file\n";
        unlink($file);
    }
}
